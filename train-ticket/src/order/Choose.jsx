import React, {
  memo
} from 'react'
import PropTypes from 'prop-types'
import './Choose.css'

const Choose = memo(function Choose (props) {
  return (
    <div>Choose</div>
  )
})

Choose.propTypes = {

}

export default Choose
