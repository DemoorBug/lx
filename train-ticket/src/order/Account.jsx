import React, {
  memo
} from 'react'
import PropTypes from 'prop-types'
import './Account.css'

const Account = memo(function Account (props) {
  return (
    <div>Account</div>
  )
})

Account.propTypes = {

}

export default Account
